/*
    unique pointer = You can only set one reference to this pointer , you cannot set more than that
    shared pointer = create more referenced. Create 2 reference for 1 pointer
    weak pointer = A pointer 

*/








#include <iostream>
#include <cstring>

//Add these for smartpointer
#include <memory>
#include <utility>

using namespace std;

int main(void)
{

    return 0;
}